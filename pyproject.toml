[tool.poetry]
name = "agent-threads"
version = "0.1.1"
description = "Rich threads for AI agents"
authors = ["Patrick Barker <patrickbarkerco@gmail.com>"]
license = "Apache 2.0"
readme = "README.md"
packages = [{include = "agent_threads"}]

[tool.poetry.dependencies]
python = "^3.10"
sqlalchemy = "^2.0.27"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
